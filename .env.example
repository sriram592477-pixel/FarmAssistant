# Server-side OpenRouter key used by the /api/gemini serverless function.
# Get a key at https://openrouter.ai/keys (starts with sk-or-v1-...).
# Local dev: copy this file to .env and fill in your key, then run `npm run dev`.
# Production: set this in Vercel → Project Settings → Environment Variables.
OPENROUTER_API_KEY=
