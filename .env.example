# Server-side Gemini key used by the /api/gemini serverless function.
# Local dev: copy this file to .env and fill in your key, then run `vercel dev`.
# Production: set this in Vercel → Project Settings → Environment Variables.
GEMINI_API_KEY=
